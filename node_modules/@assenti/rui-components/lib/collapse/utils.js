var containerStyle = {
  display: "inline-block",
  position: "absolute",
  visibility: "hidden",
  zIndex: -1
};
export var measureDomNode = function measureDomNode(node) {
  var enhanceMeasurableNode = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : function (e) {
    return e;
  };
  var container = document.createElement("div");
  container.style = containerStyle;
  var clonedNode = node.cloneNode(true);
  var content = enhanceMeasurableNode(clonedNode);
  container.appendChild(content);
  document.body.appendChild(container);
  var height = container.clientHeight;
  var width = container.clientWidth;
  container.parentNode.removeChild(container);
  return {
    height: height,
    width: width
  };
};

var enhanceMeasurableNode = function enhanceMeasurableNode(node) {
  node.style.height = "auto";
  return node;
};

export var measureSubmenu = function measureSubmenu(node) {
  return measureDomNode(node, enhanceMeasurableNode);
};