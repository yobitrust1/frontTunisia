import * as React from 'react';

declare interface DatePickerProps {
    locale?: 'en' | 'ru' | 'kz' | 'fr',
    clearable?: boolean,
    onClear?: Function,
    value: string,
    disabledDates?: Date[],
    minDate?: Date,
    maxDate?: Date,
    onChange: Function,
    placeholder?: string,
    hideWeekend?: boolean,
    hideCurrentDay?: boolean,
    label?: string,
    onDate: Function,
    active?: Date,
    dark?: boolean,
    selectBorderType?: 'tile' | 'rounded' | 'smooth',
    btnColor?: 'primary' | 'info' | 'success' | 'error' | 'black' | 'secondary',
    cancelBtnName?: string,
    inModal?: boolean,
    modalTitle?: React.ReactNode,
    width?: string | number,
    hint?: string,
    hintColor?: 'success' | 'error',
    color?: 'primary' | 'info' | 'success' | 'error',
    size?: 'medium' | 'large',
    onlyPast?: boolean,
    className?: string
}

declare const DatePicker: React.FC<DatePickerProps>;
