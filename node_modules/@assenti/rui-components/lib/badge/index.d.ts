import * as React from "react";

declare interface BadgeProps {
    rounded?: boolean,
    icon?: string,
    onClick?: Function,
    color?: 'primary' | 'info' | 'success' | 'error' | 'dark' | 'secondary',
    value: string | number,
    visible?: boolean,
    parent?: React.ReactNode,
    className?: string
}
declare const Badge: React.FC<BadgeProps>;