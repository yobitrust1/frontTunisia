import * as React from 'react';

declare interface TimePickerProps {
    locale?: 'en' | 'ru' | 'kz',
    clearable?: boolean,
    onClear?: Function,
    value: string,
    onChange: Function,
    placeholder?: string,
    label?: string,
    inModal?: boolean,
    width?: string | number,
    hint?: string,
    dark?: boolean,
    hintColor?: 'success' | 'error',
    color?: 'primary' | 'info' | 'success' | 'error',
    size?: 'medium' | 'large',
    className?: string
}

declare const TimePicker: React.FC<TimePickerProps>;