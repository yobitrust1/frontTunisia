import * as React from 'react';
declare interface SnackbarOptions {
    type?: 'info' | 'success' | 'error',
    borderType?: 'tile' | 'rounded',
    dark?: boolean,
    duration?: number,
    controls?: React.ReactNode
}
declare type SnackbarFunction = (message: string, options?: SnackbarOptions) => void;

declare interface SnackbarObject {
    message: SnackbarFunction,
    remove: Function
}

declare const snackbar: SnackbarObject;  
declare const Snackbar: React.FC<any>;