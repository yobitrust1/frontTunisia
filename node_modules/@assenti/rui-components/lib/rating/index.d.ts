import * as React from 'react';

declare type RatingColor = 'yellow' | 'primary' | 'info' | 'success' | 'error';

declare interface RatingProps {
    hideRate?: boolean,
    size?: number,
    rate?: number,
    count?: number,
    disabled?: boolean,
    onChange?: Function,
    text?: string,
    color?: RatingColor,
    className?: string
}

declare const Rating: React.FC<RatingProps>;