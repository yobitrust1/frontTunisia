"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _classCallCheck2 = _interopRequireDefault(require("@babel/runtime/helpers/classCallCheck"));

var _createClass2 = _interopRequireDefault(require("@babel/runtime/helpers/createClass"));

var PageNodeItem = require('./node');
/**
 * Tree page object class.
 * @class
 */


var PageTree = /*#__PURE__*/function () {
  /**
   * Create a new Tree page object.
   * @constructor
   * @param {string} rootElement - The selector of the Tree root element.
   */
  function PageTree(rootElement) {
    (0, _classCallCheck2["default"])(this, PageTree);
    this.rootElement = rootElement;
  }
  /**
   * Returns a new Node page object of the element at specified path.
   * @method
   * @param {array} path - Array with 0 base indexes that defines the node path in tree.
   */


  (0, _createClass2["default"])(PageTree, [{
    key: "getNode",
    value: function getNode(path) {
      var nodePath = path.join('.');
      var node = $(this.rootElement).$("[data-path=\"".concat(nodePath, "\"]"));

      if (node) {
        return new PageNodeItem("".concat(this.rootElement, " [data-path=\"").concat(nodePath, "\"]"));
      }

      return null;
    }
  }]);
  return PageTree;
}();

module.exports = PageTree;